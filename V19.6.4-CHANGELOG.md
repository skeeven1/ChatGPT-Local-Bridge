# V19.6.4 Runtime Boot Fix

## Baseline
V19.6.3 started the new runtime but failed loading Agent Runtime on Windows ESM paths and kept schema metadata at 19.0.0.

## Hypothesis
The remaining blockers are:
- Windows ESM file URL conversion;
- duplicated hardcoded version metadata.

## Changes
- use pathToFileURL for runtime imports;
- synchronize exposed schema version to 19.6.4;
- update validation metadata.

## Target metrics
- Agent Runtime loads successfully;
- OpenAPI/preflight version matches runtime;
- no regression of existing HTTP routes.

## Decision
KEEP pending Windows runtime validation.
